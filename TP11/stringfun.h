#ifndef STRINGFUN_H
#define STRINGFUN_H
void reshape(char *string,char *destino);  //elimina espacios y signos de una cadena de caracteres
int wordlength(char *word);     //calcula la cantidad de caracteres de un string
void reverse (char *string,char *reves);      //copia el string recibido por el primer puntero al revés en la posición indicada por el segundo puntero
void resize (char *word);       //convierte caracteres de mayúscula a minúscula
#endif

